const env = process
.env
.
NODE_ENV;

process.
env.
   EXTRA

process
.
env
.
VALUE_0;

const example1 = process.env.VALUE_1 + process.env.VALUE_2;

const example2 = process.env.VALUE_1 + process.env.
VALUE_3;

function main() {
  console.log(process
    .env
    .PWD);
  console.log(process.
    env.
    PWD);
}

main();